---
title: "ResNet & Residual Learning"
description: "The degradation problem, residual learning, basic/bottleneck blocks, projection shortcuts, pre-activation, and residual as a universal design pattern."
lang: en
category: architectures
order: 2
parent: architecture-anatomy
translation: resnet
tags: [resnet, residual, bottleneck, skip-connection, pre-activation]
---

import Note from '../../components/Note.astro';
import ResidualGradientViz from '../../components/ResidualGradientViz.astro';
import ResidualBlockDiagram from '../../components/ResidualBlockDiagram.astro';

# ResNet & Residual Learning

The [Convolutional Networks](/dl-anatomy/architectures/cnn-en) article ended on a paradox: in terms of representation, a deeper network should **not** be worse than a shallower one — if the extra layers could learn the identity, the deep net could just mimic the shallow one. But in practice, when a CNN gets too deep, *training error rises too*. This is the **degradation problem**: the issue isn't overfitting, it's that deep nets become **hard to optimize**.

## Why very deep CNNs failed

This is *not* overfitting — overfitting means good train, bad test; here both are bad. The core issue is **optimization degradation**: a deeper network can represent more in theory, but is harder to train in practice.

Vanishing/exploding gradients are part of this intuition — gradients propagating back through many layers are multiplied repeatedly and tend to shrink to zero or blow up — but they're **not** the only explanation. What ResNet does more deeply is change the *learning problem*: from directly learning a mapping $H(x)$ to learning the **residual** $F(x) = H(x) - x$, which makes the optimization landscape easier to traverse.

## Residual learning

**He et al. (2015)** made a small but profound change: instead of learning $H(x)$ directly, each block learns the residual $F(x)$ and adds back the input:

$$
y = F(x) + x
$$

During backprop, the Jacobian of the output w.r.t. the input gains an identity branch:

$$
\frac{\partial y}{\partial x} = \frac{\partial F(x)}{\partial x} + I
$$

So the gradient of the loss $L$ flowing back to $x$ is:

$$
\frac{\partial L}{\partial x} = \frac{\partial L}{\partial y}\left( \frac{\partial F(x)}{\partial x} + I \right)
$$

The $I$ term creates a direct gradient path through the identity branch — $\frac{\partial L}{\partial y} I$. It doesn't guarantee gradients are always perfect, but it makes the backward signal far easier to propagate through many blocks than in a plain stack with no skip connections. Together with BatchNorm and a suitable block design, residual connections let ResNet train very deep networks — **over 1000 layers** in experiments.

<ResidualBlockDiagram lang="en" />

<ResidualGradientViz lang="en" />

<Note type="result">
Skip connections also make the **identity easy to learn**: if a block isn't needed, it can drive $F(x) \to 0$ so the addition returns close to identity. In ResNet v1 there is still a ReLU after the Add, so the cleanest identity path is really in pre-activation v2; but compared with a plain convolutional stack, the "do nothing extra" solution is much easier to reach. Representationally, a deep net can at least mimic a shallower one if the extra blocks learn near-identity — and residual connections make that solution **much easier to reach**, sharply reducing the degradation problem in practice.
</Note>

## Anatomy of a residual block

### Terminology

These terms get used loosely; to be precise:

- **Skip / shortcut connection**: a path that bypasses a few layers.
- **Identity shortcut**: a shortcut that carries $x$ unchanged (no parameters).
- **Projection shortcut**: a shortcut with a projection (usually a $1\times1$ conv) when shapes don't match.
- **Residual block**: a block that learns $F(x)$ and adds it to the shortcut.

### Basic block

In ResNet v1, the basic block — used in **ResNet-18 / ResNet-34** — is two $3\times3$ convolutions:

```
x → [3×3 conv → BN → ReLU → 3×3 conv → BN] → (+x) → ReLU
```

### Bottleneck block

Used in **ResNet-50 / 101 / 152** — a $3\times3$ conv sandwiched between two $1\times1$ convs:

```
x → [1×1 conv (reduce) → 3×3 conv → 1×1 conv (expand)] → (+x) → ReLU
```

The diagram omits BN/ReLU to keep the channel structure visible. In ResNet v1, each convolution inside the bottleneck is usually paired with BN and ReLU, except the final convolution, which has BN before adding the shortcut.

The first $1\times1$ conv **reduces** channels, the $3\times3$ conv processes space on fewer channels (much cheaper), and the last $1\times1$ conv **expands** channels back. It cuts FLOPs substantially while keeping expressiveness — exactly the $1\times1$ idea borrowed from Inception (see [Convolutional Networks](/dl-anatomy/architectures/cnn-en)).

In the common implementation, the bottleneck uses an **expansion ratio of 4**: if the middle has 64 channels, the block's output has 256.

### When shapes don't match

The addition $F(x) + x$ only works when both sides have the **same shape**. When a block changes stage — more channels or lower resolution (stride 2) — the skip branch must use a **projection shortcut**:

$$
y = F(x) + W_s\, x
$$

where $W_s$ is usually a $1\times1$ conv, with stride to match dimensions.

## ResNet v1 vs pre-activation (v2)

The original ResNet (v1) places activation **after** the addition:

```
Conv → BN → ReLU → Conv → BN → Add → ReLU
```

**Pre-activation ResNet (v2)** — *Identity Mappings in Deep Residual Networks* — moves BN/ReLU **before** the convolutions:

```
BN → ReLU → Conv → BN → ReLU → Conv → Add
```

This keeps the identity branch "clean" (no activation wedged in after the Add), so the forward signal and backward gradient pass through the skip path more directly — making extremely deep nets easier to train and slightly better at generalizing.

| Variant | Block | For | Key idea |
|---|---|---|---|
| ResNet-18 / 34 | Basic | small/medium models | two $3\times3$ convs |
| ResNet-50 / 101 / 152 | Bottleneck | deep models | $1\times1$ reduce → $3\times3$ → $1\times1$ expand |
| ResNet v2 | Pre-activation | very deep models | BN/ReLU before conv, cleaner identity path |

<Note type="warning">
**BatchNorm** in the blocks above is an important part of the *block design* — especially when comparing ResNet v1 and pre-activation v2, where BN placement makes a difference. BatchNorm, LayerNorm, and RMSNorm are covered systematically in [Normalization Layers](/dl-anatomy/architectures/normalization-layers-en).
</Note>

## How ResNet changed the way we think about layers

Before ResNet, we thought each stack of layers had to learn a brand-new mapping $H(x)$. ResNet reframed it: each block only needs to learn a **small update** $F(x)$ to the current representation.

$$
\text{new representation} = \text{old representation} + \text{learned update}
$$

This "refine gradually rather than replace wholesale" mindset reappears everywhere: the residual path in **Transformers**, U-Net, neural ODEs, adapters in fine-tuning (a residual update on the *representation*), and LoRA at the weight level (a low-rank update on existing *weights*).

## After ResNet: CNNs keep evolving

ResNet made *depth* trainable and turned residual + bottleneck into the default baseline. But "deeper" isn't the only path — CNNs then evolved along several design axes: **cardinality** (ResNeXt), **connectivity** (DenseNet), **efficiency** (MobileNet/ShuffleNet), **scaling** (EfficientNet), and **modernization** (ConvNeXt).

That's a separate story about allocating *compute* more cleverly — see [Modern CNNs: From ResNeXt to ConvNeXt](/dl-anatomy/architectures/modern-cnns-en).

## Residual beyond CNNs

The idea of creating a **shorter path for information and gradients** shows up in many places:

- **Residual path** in the Transformer block (standard in every LLM)
- **Skip connections** in U-Net (segmentation, diffusion)
- **Cell state** in the LSTM

They aren't mechanically identical — the LSTM cell state uses multiplicative gates, not a pure additive identity — but they serve the same intuition: help information and gradients travel through many steps **more easily**.

<Note type="result">
ResNet made **depth practical**, and more importantly turned residual learning into a **universal design pattern**: learn a small update to the current representation rather than replacing it wholesale.
</Note>

## References

| Paper | |
|---|---|
| He et al. (2015) | Deep Residual Learning for Image Recognition (ResNet v1) |
| He et al. (2016) | Identity Mappings in Deep Residual Networks (pre-activation, v2) |
