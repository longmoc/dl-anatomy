---
title: "State Space Models (Mamba)"
description: "SSM đưa recurrence tuyến tính trở lại sequence modeling: train song song như convolution, infer gọn như RNN, và Mamba thêm selective scan để cạnh tranh với attention."
lang: vi
category: architectures
order: 5
parent: architecture-anatomy
translation: state-space-models-en
tags: [ssm, mamba, sequence, linear-attention]
---

import Note from '../../components/Note.astro';
import SSMTwoViewsDiagram from '../../components/SSMTwoViewsDiagram.astro';
import SelectiveScanDiagram from '../../components/SelectiveScanDiagram.astro';
import AttnVsSSMMemoryDiagram from '../../components/AttnVsSSMMemoryDiagram.astro';

# State Space Models (Mamba)

Bài [Sequence Models](/dl-anatomy/architectures/sequence-models) kết thúc ở một ngã rẽ: RNN/LSTM giữ được **state nén** nhưng khó song song hóa theo thời gian; Transformer bỏ recurrence để train song song, nhưng attention chuẩn tốn $O(n^2)$ và KV-cache tăng theo độ dài context.

**State Space Models (SSM)** đi theo hướng thứ ba: giữ lại ý tưởng state của RNN, nhưng làm state đó đủ có cấu trúc để vừa mô hình hóa chuỗi dài, vừa tính toán hiệu quả. Nếu RNN là "state tự do", Transformer là "memory bank đầy đủ", thì SSM là "state động lực học có kiểm soát".

## Từ hệ động lực học đến sequence model

SSM đến từ control theory. Ta bắt đầu với một hệ liên tục theo thời gian:

$$
\frac{dh(t)}{dt} = A h(t) + B x(t), \qquad y(t) = C h(t)
$$

Trong đó:

- $x(t)$ là input đi vào hệ
- $h(t)$ là state ẩn, giữ lịch sử đã nén
- $y(t)$ là output đọc ra từ state
- $A$ điều khiển state tự tiến hóa và "quên" như thế nào
- $B$ điều khiển input được ghi vào state ra sao
- $C$ điều khiển phần nào của state được đọc ra

Dữ liệu deep learning thường là chuỗi rời rạc: token, frame âm thanh, timestep time-series. Vì vậy hệ liên tục được **discretize** thành:

$$
h_t = \bar{A} h_{t-1} + \bar{B} x_t, \qquad y_t = C h_t
$$

Ký hiệu $\bar{A}, \bar{B}$ nhắc rằng đây là phiên bản sau rời rạc hóa của $A, B$. Nhìn công thức này, SSM rất giống RNN. Điểm khác biệt then chốt: cập nhật state là **tuyến tính** theo $h_{t-1}$ và $x_t$.

<Note type="intuition">
RNN học một hàm cập nhật phi tuyến tự do: "state mới là gì?". SSM học một hệ động lực học có cấu trúc: "state nên suy giảm, giữ lại và ghi thêm thông tin theo quy luật nào?". Chính cấu trúc tuyến tính này mở ra cách tính song song.
</Note>

## Vì sao tuyến tính lại quan trọng

Với recurrence tuyến tính, ta có thể unroll vài bước để thấy pattern:

$$
h_1 = \bar{B} x_1
$$

$$
h_2 = \bar{A}\bar{B}x_1 + \bar{B}x_2
$$

$$
h_3 = \bar{A}^2\bar{B}x_1 + \bar{A}\bar{B}x_2 + \bar{B}x_3
$$

Khi đọc output:

$$
y_t = C h_t = \sum_{k=0}^{t-1} C\bar{A}^{k}\bar{B}x_{t-k}
$$

Nói cách khác, output là một phép **convolution** giữa input và một kernel dài:

$$
y = x * \bar{K}, \qquad \bar{K} = (C\bar{B},\; C\bar{A}\bar{B},\; C\bar{A}^2\bar{B},\; \dots)
$$

Đây là điểm đẹp nhất của SSM: cùng một mô hình có hai cách nhìn.

| Cách nhìn | Dùng khi | Ý nghĩa |
|---|---|---|
| Convolution / kernel | Training | xử lý cả chuỗi song song, giống một convolution 1D rất dài |
| Recurrence / state update | Inference | chạy từng token với state cố định, không cần giữ toàn bộ quá khứ |

Với S4 và các SSM có cấu trúc, kernel có thể được tính và áp dụng rất hiệu quả, thường gần tuyến tính theo độ dài chuỗi trong implementation hiện đại. Với Mamba, selective scan được thiết kế để chạy tuyến tính theo sequence length.

<SSMTwoViewsDiagram />

## SSM khác RNN và Transformer ở đâu?

RNN, Transformer và SSM đều là cách trả lời câu hỏi: **token hiện tại nên dùng quá khứ như thế nào?**

| Kiến trúc | Bộ nhớ quá khứ | Chi phí theo độ dài | Điểm mạnh | Điểm yếu |
|---|---|---|---|---|
| RNN/LSTM | state cố định | $O(n)$ nhưng tuần tự | infer gọn, hợp chuỗi streaming | train khó song song, phụ thuộc dài khó |
| Transformer | KV-cache / attention đến mọi token | $O(n^2)$ cho attention chuẩn | truy xuất nội dung rất linh hoạt | bộ nhớ và compute tăng nhanh |
| SSM/Mamba | state cố định có cấu trúc | tuyến tính hoặc gần tuyến tính theo chuỗi | chuỗi dài, streaming, inference gọn | state nén có thể mất chi tiết cần truy xuất chính xác |

Attention giữ một memory bank rõ ràng: mỗi token quá khứ để lại key/value riêng. SSM không giữ mọi token; nó nén quá khứ vào state. Vì vậy SSM thường rất hấp dẫn cho audio, genomics, time-series, long-context và các workload cần streaming.

<AttnVsSSMMemoryDiagram />

<Note type="warning">
State cố định không phải "miễn phí". Nó là một bottleneck có chủ đích. Nếu task cần truy xuất chính xác một chi tiết xa trong quá khứ, attention vẫn có lợi thế tự nhiên vì nó có thể nhìn trực tiếp từng token cũ.
</Note>

## S4 — làm state dài trở nên train được

Nếu chỉ viết công thức tuyến tính phía trên, câu hỏi lớn vẫn còn: làm sao chọn $A$ để state vừa ổn định, vừa nhớ được dependency dài?

**S4 (Structured State Space Sequence Model)** giải quyết bằng cách đặt cấu trúc đặc biệt lên ma trận state. Thay vì học một ma trận $A$ tùy ý, S4 dùng các tham số hóa có tính ổn định và tính toán hiệu quả, liên quan đến HiPPO — một cách nén lịch sử liên tục vào state sao cho giữ được thông tin dài hạn.

Điều quan trọng ở mức architecture:

- S4 cho thấy SSM không chỉ là RNN tuyến tính đơn giản.
- State có thể được thiết kế để giữ lịch sử dài một cách có kỷ luật.
- Khi SSM có cấu trúc tốt, nó có thể cạnh tranh ở những chuỗi rất dài nơi attention chuẩn đắt đỏ.

Ta không cần đi sâu vào đại số của S4 ở bài anatomy này. Chỉ cần nhớ: **S4 là bước chứng minh rằng state-space layer có thể trở thành một block sequence modeling nghiêm túc, không chỉ là ý tưởng từ control theory.**

## Mamba — selective state

SSM cổ điển có một điểm yếu lớn: các tham số như $\bar{A}, \bar{B}, C$ thường cố định sau khi học. Điều đó nghĩa là layer dùng cùng một dynamics cho mọi token. Nhưng trong ngôn ngữ, code hay audio, không phải token nào cũng quan trọng như nhau.

Attention xử lý chuyện này rất tự nhiên: nội dung của token quyết định nó attention đến đâu. Mamba đưa một phần tính **content-dependent** đó vào SSM.

Trong Mamba, các tham số đọc/ghi state phụ thuộc vào input:

$$
\Delta_t = \Delta(x_t), \qquad B_t = B(x_t), \qquad C_t = C(x_t)
$$

- $\Delta_t$ điều khiển bước rời rạc hóa: state nên thay đổi nhanh hay chậm ở token này
- $B_t$ điều khiển token hiện tại được ghi vào state thế nào
- $C_t$ điều khiển model đọc phần nào từ state để tạo output

Trực giác: Mamba không chỉ có một bộ lọc cố định chạy qua chuỗi. Nó để mỗi token quyết định **ghi gì, giữ gì, đọc gì**.

<SelectiveScanDiagram />

Đổi lại, khi $B_t, C_t, \Delta_t$ phụ thuộc vào input, ta không còn một convolution kernel cố định như S4 cổ điển. Mamba vì vậy dùng **selective scan**: một thuật toán scan song song được tối ưu cho GPU, giữ chi phí tuyến tính theo độ dài chuỗi nhưng vẫn train hiệu quả.

<Note type="result">
Mamba quan trọng không chỉ vì "không dùng attention". Điểm chính là nó biến recurrence tuyến tính thành một block hiện đại: content-dependent, chạy tốt trên GPU, infer gọn, và hợp với chuỗi dài.
</Note>

## Mamba block trong neural network

Trong model thực tế, Mamba không đứng một mình như một phương trình state-space trần. Nó được đóng gói thành một block neural network, thường gồm:

1. projection từ embedding vào hidden channels
2. convolution ngắn theo chiều sequence để trộn local context
3. selective SSM / selective scan để truyền thông tin dài hạn
4. gating và output projection
5. residual connection và normalization quanh block

Điều này làm Mamba gần với cách ta nghĩ về Transformer block: không chỉ có một operation lõi, mà là một **module** có mixing, gating, residual path và normalization. Operation lõi khác nhau:

| Block | Token mixing chính | Memory |
|---|---|---|
| Transformer block | self-attention | KV-cache theo token |
| Mamba block | selective scan | state cố định |

Nếu Transformer hỏi "token này nên nhìn token nào?", Mamba hỏi "token này nên cập nhật state như thế nào?".

## Vị trí hiện tại của SSM

SSM/Mamba không nên được hiểu là "Transformer killer" đơn giản. Đúng hơn, nó là một nhánh kiến trúc rất mạnh cho các bài toán mà chi phí attention trở thành nút thắt.

SSM đặc biệt hợp khi:

- chuỗi rất dài và attention $O(n^2)$ quá đắt
- cần inference streaming với bộ nhớ cố định
- dữ liệu có tính tín hiệu liên tục như audio, time-series, genomics
- muốn giảm KV-cache trong long-context model

Attention vẫn rất mạnh khi:

- cần truy xuất chính xác chi tiết xa
- cần so khớp linh hoạt giữa mọi cặp token
- workload đã được tối ưu sâu quanh Transformer

Vì vậy nhiều hướng thực tế dùng **hybrid architectures**: xen kẽ attention layer và SSM layer. Attention đảm nhận truy xuất nội dung sắc nét; SSM đảm nhận truyền thông tin dài với chi phí tuyến tính.

## Ba cách nhìn sequence modeling

Thay vì xem lịch sử là một đường thẳng duy nhất, nên nhìn sequence architecture như ba họ ý tưởng:

```text
Recurrence family:    RNN -> LSTM/GRU
Attention family:     Seq2Seq attention -> Transformer
State-space family:   S4 -> Mamba -> Mamba-2 / hybrid SSM
```

Ba họ này không thay thế nhau sạch sẽ. Chúng là ba cách đánh đổi giữa **bộ nhớ**, **song song hóa**, **khả năng truy xuất**, và **chi phí inference**.

Bài tiếp theo quay lại nhánh attention: [Transformer & Attention](/dl-anatomy/architectures/transformer), nơi bộ nhớ quá khứ không bị nén vào state cố định mà mở ra thành quan hệ trực tiếp giữa mọi cặp token.

## Papers tham khảo

| Paper | |
|---|---|
| Gu et al. (2021) | Efficiently Modeling Long Sequences with Structured State Spaces (S4) |
| Gu & Dao (2023) | Mamba: Linear-Time Sequence Modeling with Selective State Spaces |
| Dao & Gu (2024) | Transformers are SSMs: Generalized Models and Efficient Algorithms Through Structured State Space Duality |
