---
title: "Loss Landscape & Non-convexity"
description: "Why neural networks aren't convex but still optimize effectively — from saddle points to mode connectivity."
lang: en
category: optimization
order: 1
translation: loss-landscape
tags: [convexity, loss-landscape, saddle-points, local-minima, optimization]
---

import Note from '../../components/Note.astro';
import LossLandscapeViz from '../../components/LossLandscapeViz.astro';
import LocalMinimaViz from '../../components/LocalMinimaViz.astro';
import WignerViz from '../../components/WignerViz.astro';
import DoubleDescentViz from '../../components/DoubleDescentViz.astro';

# Loss Landscape & Non-convexity

## Background: convexity in classical optimization

In classical ML (linear regression, logistic regression, SVM), the first step is often to verify whether the loss function is convex:

- **Convex function**: every local minimum is a global minimum → gradient descent is guaranteed to converge to the best solution
- **Non-convex function**: may get stuck at a local minimum → no guarantees

The convexity check: the Hessian $H = \nabla^2 \mathcal{L}(\theta)$ must be **positive semi-definite** everywhere — all eigenvalues $\geq 0$.

For multi-layer neural networks, the loss is almost certainly **not convex**. Yet in practice, nobody checks convexity before training GPT-4. Why?

<LossLandscapeViz lang="en" />

Left: convex loss — GD always finds the global minimum. Right: non-convex loss — GD gets stuck; SGD escapes via noise.

## High dimensionality → saddle points, not local minima

A critical point ($\nabla\mathcal{L} = 0$) is a **local minimum** only if all $d$ eigenvalues of the Hessian are positive — i.e. the landscape curves upward in every direction. A **single** negative eigenvalue makes it a saddle point: curving up in some directions, down in others.

If we treat each eigenvalue as independently positive with probability $\frac{1}{2}$:

$$
P(\text{local minimum}) = \left(\frac{1}{2}\right)^d
$$

| Parameters $d$ | $P(\text{local minimum})$ |
|---|---|
| 10 | $\approx 0.1\%$ |
| 100 | $\approx 10^{-30}$ |
| 1000 | $\approx 10^{-300}$ |
| $10^9$ (small LLM) | $\approx 10^{-300{,}000{,}000}$ |

For networks with millions to billions of parameters, the probability that a random critical point is a local minimum is essentially zero. **Almost all critical points in large networks are saddle points** — the core difference from low-dimensional optimization, where bad local minima are the main concern.

<Note type="warning">
This is a simplified probabilistic argument — in reality the Hessian's eigenvalues are **not independent**. But the qualitative conclusion (saddle points dominate in high dimensions) holds and is confirmed by Random Matrix Theory.
</Note>

### Random Matrix Theory — Wigner Semicircle Law

Why is the "each eigenvalue positive with probability $\frac{1}{2}$" approximation reasonable? Random Matrix Theory tells us that the eigenvalue distribution of a random symmetric matrix (like the Hessian of a randomly initialized network) follows the **Wigner Semicircle Law**:

$$
\rho(\lambda) = \frac{1}{2\pi\sigma^2}\sqrt{4\sigma^2 - \lambda^2}, \quad \lambda \in [-2\sigma,\, 2\sigma]
$$

Eigenvalues concentrate in $[-2\sigma, 2\sigma]$, and the distribution is **symmetric around 0** — roughly half the eigenvalues are negative, half positive. A random critical point almost always has both upward- and downward-curving directions → it's a saddle point.

But the spectrum **shifts with loss**: at a high-loss critical point (far from the global min), the distribution is balanced around 0 → index $\approx 0.5$ → a steep saddle. At a low-loss critical point (near the global min), the spectrum shifts toward positive → fewer negative eigenvalues → close to a true local minimum.

<WignerViz lang="en" />

**Dauphin et al. (2014)** — the seminal paper on this problem — proved exactly that relationship: the **index** of a critical point (fraction of negative eigenvalues) strongly correlates with the loss at that point. The lower the loss, the more the point resembles a good local minimum — and bad saddle points mostly sit in high-loss regions.

### Saddle point vs plateau

Both produce small gradients, but the mechanism and the fix differ:

| | Saddle point | Plateau |
|---|---|---|
| Gradient $\nabla\mathcal{L}$ | $= 0$ | $\approx 0$ |
| Hessian | Has negative eigenvalues (escape directions) | All eigenvalues $\approx 0$ |
| Nature | Saddle — there's a way down | Flat region — no clear direction |
| Fix | SGD noise (projects onto negative directions) | Momentum, Adam (accumulate steps) |

### How gradient descent escapes saddle points

- **Exact GD (full-batch)**: at a saddle point $\nabla\mathcal{L} = 0$ exactly → zero update → **stuck** indefinitely in theory.
- **SGD**: the noise $\epsilon_t$ in the gradient projects onto the Hessian's negative-eigenvalue directions → pushes θ off the saddle along the downward-curving direction → **escapes naturally**, with no intervention.
- **Perturbed GD (Jin et al., 2017)**: adds deliberate noise when the gradient is small → provably escapes saddle points in polynomial time, with rigorous theoretical guarantees.

The escape speed is proportional to the **magnitude of the negative eigenvalue** (negative curvature): a "steep" saddle (large negative eigenvalue) is escaped quickly; a "flat" saddle (small negative eigenvalue) is escaped slowly — this is precisely the **vanishing gradient** phenomenon viewed from the landscape angle. Conversely, **Du et al. (2017)** proved that full-batch GD can, in the worst case, take **exponential time** to escape a saddle — further underscoring the role of noise in SGD.

<Note type="result">
This is a key reason SGD outperforms full-batch GD on non-convex losses: noise isn't a flaw to be removed, but a mechanism for **automatically escaping saddle points** — something full-batch GD lacks entirely.
</Note>

## All good local minima are equivalent

### Permutation symmetry

Consider a simple network with 2 interchangeable neurons:

```
Input → [neuron A, neuron B] → Output
Swap weights of A and B:
  → Same function
  → Same loss value
  → Different point in parameter space
```

A network with $L$ layers and $n$ neurons per layer has $n!^L$ such points, all with **identical loss**. This is the simplest example of a deeper phenomenon: parameter space contains countless "equally good" points due to the network's permutation symmetry.

### Choromanska et al. (2015) — spin-glass model

*"The Loss Surfaces of Multilayer Networks"* was one of the first papers to systematically characterize this. The key idea: under some simplifying assumptions, the neural network loss surface maps onto a **spin-glass model** from statistical physics — a well-studied physical system.

The result: for $n \to \infty$:

$$
\mathcal{L}(\theta^*_{\text{local}}) \approx \mathcal{L}(\theta^*_{\text{global}})
$$

Local minima distribute in a pattern: bad local minima cluster in the low-complexity (underfitting) regime; good local minima cluster in the overparameterized regime.

<LocalMinimaViz type="spinglass" lang="en" />

<Note type="warning">
The paper makes strong simplifying assumptions that don't fully match modern transformers. But its intuition and empirical observations have been widely confirmed since.
</Note>

### Mode Connectivity — Garipov et al. (2018)

Good local minima don't just have equivalent loss — they're **connected to each other** by paths along which the loss stays low.

**Experiment**: train the same network twice with different random seeds → $\theta_A$ and $\theta_B$. Two ways to connect them:

**Linear interpolation** (straight line):

$$
\theta(t) = (1-t)\theta_A + t\theta_B, \quad t \in [0,1]
$$

→ Loss **spikes** in the middle — the two minima are separated by a high barrier.

**Bézier curve** through an optimized midpoint $\theta_{\text{mid}}$:

$$
\theta(t) = \text{BezierCurve}(\theta_A,\; \theta_B,\; \theta_{\text{mid}})
$$

→ Loss **barely changes** throughout the journey from $\theta_A$ to $\theta_B$.

<LocalMinimaViz type="mode" lang="en" />

**Draxler et al. (2018)** generalized further: in large enough networks, minima connect via **straight lines** (no curved path needed). **Benton et al. (2021)** extended this to *simplexes* — not just curves but entire planes and subspaces connecting minima at low loss.

<Note type="theorem">
Good local minima aren't isolated holes — they lie on the same **connected flat region** in parameter space. "Many minima" is really one large region that gradient descent, once it enters, stays stable in.
</Note>

### Flat vs Sharp — not all good minima are equal

Everything above argues that good local minima are "equivalent" — but there is one important distinction: not all good local minima **generalize** equally well. Measured by the Hessian:

$$
\mathcal{L}(\theta + \epsilon) - \mathcal{L}(\theta) \approx \frac{1}{2}\epsilon^T H \epsilon
$$

<LocalMinimaViz type="flatsharp" lang="en" />

| | Sharp minimum | Flat minimum |
|---|---|---|
| Eigenvalues of $H$ | Large | Small |
| Small perturbation $\epsilon$ | Large loss increase | Stable loss |
| Distribution shift (train→test) | Test loss spikes → overfits | Test loss barely changes → generalizes well |
| Found by SGD? | Rarely — noise "shakes it out" | Frequently — occupies larger volume |

#### Why SGD is biased toward flat minima

The probability of stopping at a minimum is proportional to the **volume** of the surrounding region:

$$
p(\text{stopping at minimum}) \propto \frac{1}{\sqrt{\det H}}
$$

A flat minimum has small $\det H$ → higher stopping probability — not because SGD "knows" flat is better, but simply because flat minima **occupy more volume** in parameter space.

**Keskar et al. (2017)** confirmed this empirically: small batch → more noise → SGD finds flat minima → better generalization. Large batch → less noise → sharp minima → worse generalization.

**Hochreiter & Schmidhuber (1997)** — the classic paper formally defining flat minima and connecting them to MDL (Minimum Description Length): a flat minimum corresponds to a model with **lower description complexity**.

## SGD noise as a natural regularizer

In classical ML, a regularizer is added **explicitly** to the loss:

$$
\mathcal{L}_{\text{reg}}(\theta) = \mathcal{L}(\theta) + \lambda\|\theta\|^2
$$

SGD does the same thing on its own — but **implicitly**, with no extra term.

### The structure of SGD noise

The gradient SGD actually uses is a stochastic estimate of the true gradient:

$$
\tilde{g}_t = \nabla\mathcal{L}(\theta_t) + \epsilon_t
$$

$$
\text{Cov}(\epsilon_t) = \frac{1}{|B|}\Sigma(\theta_t)
$$

where $\Sigma(\theta_t) = \text{Cov}_{i \sim \text{data}}[\nabla\ell_i(\theta_t)]$ is the **gradient covariance matrix** — a property of the data distribution at the current position $\theta_t$.

Two important properties:

**1. Noise is inversely proportional to batch size** — $\text{Var}(\epsilon_t) \propto \frac{1}{|B|}$. Smaller batch → more noise → stronger regularization.

**2. Noise depends on position $\theta$** — in low-loss regions, individual gradients $\nabla\ell_i$ tend to agree → $\Sigma$ small → little noise → stable. At a sharp minimum, the $\nabla\ell_i$ scatter → $\Sigma$ large → more noise → the model gets pushed away. This is a self-stabilizing mechanism.

### SGD ≈ Langevin Dynamics

When the learning rate is small and steps are treated as continuous, SGD approximates a **Stochastic Differential Equation (Langevin dynamics)**:

$$
d\theta = -\nabla\mathcal{L}(\theta)\,dt + \sqrt{\eta \cdot \Sigma(\theta)}\,dW_t
$$

where $dW_t$ is a Wiener process (Brownian motion). The system's effective temperature:

$$
T_{\text{eff}} \propto \frac{\eta}{|B|}
$$

The **stationary distribution** of this Langevin system:

$$
p(\theta) \propto \exp\!\left(-\frac{\mathcal{L}(\theta)}{T_{\text{eff}}}\right)
$$

This is exactly the **Gibbs distribution** from statistical physics — and the **Bayesian posterior** under a uniform prior on $\theta$.

<Note type="theorem">
SGD doesn't just find a point $\theta^*$ — theoretically, it **samples from the posterior** over $\theta$ at "temperature" $T_{\text{eff}} = \eta/|B|$. Large learning rate, small batch → high temperature → SGD explores the landscape broadly. Small learning rate, large batch → low temperature → SGD sticks close to the current region.
</Note>

### Implicit regularizer

The expected loss after one SGD step with noise $\epsilon$:

$$
\mathbb{E}[\mathcal{L}(\theta_{t+1})] \approx \mathcal{L}(\theta_t) - \eta\|\nabla\mathcal{L}\|^2 + \underbrace{\frac{\eta^2}{2}\,\text{tr}(H\Sigma)}_{\text{noise penalty}}
$$

The term $\frac{\eta^2}{2}\,\text{tr}(H\Sigma)$ is an **implicit regularizer**: large when $H$ is large (sharp region) → SGD is penalized for staying in high-curvature areas; small when $H$ is small (flat region) → SGD is comfortable there. No need to add $\lambda\|\theta\|^2$ — SGD automatically penalizes sharp regions.

### Linear scaling rule

From $T_{\text{eff}} \propto \frac{\eta}{|B|}$:

> Increasing batch size by $k$ ≡ decreasing temperature by $k$ → learning rate must increase by $k$ to compensate. This is why, when training across many GPUs (larger batch), practitioners scale LR by the number of GPUs — not folklore, but grounded in the SDE theory.

| Batch size | Converges to | Generalization |
|---|---|---|
| Small | Flat minima | Good |
| Large | Sharp minima | Worse |

## Overparameterization creates a solution space

It sounds paradoxical: adding parameters makes the search space **larger**, so why is optimization easier? The answer lies in how the nature of the problem changes:

- When $|\theta| \ll n_{\text{data}}$: too few parameters to fit the data → information must be "compressed" → easy to get stuck at bad local minima
- When $|\theta| \gg n_{\text{data}}$: **infinitely many ways** to fit the data → the landscape changes completely

### From a system of equations to a solution manifold

At the data-point level, training solves $n$ conditions $f_\theta(x_i) = y_i$, each a hypersurface in parameter space $\mathbb{R}^{|\theta|}$:

| Regime | Solution set |
|---|---|
| $|\theta| < n$ | Overconstrained — usually **no exact solution** |
| $|\theta| = n$ | Just enough — usually **finitely many isolated solutions** |
| $|\theta| \gg n$ | Underconstrained — solutions form a high-dimensional **manifold** |

In the overparameterized regime, the dimension of the solution manifold:

$$
\dim(\mathcal{M}^*) \approx |\theta| - n
$$

| Model | $|\theta|$ | $n_{\text{data}}$ (approx) | $\dim(\mathcal{M}^*)$ |
|---|---|---|---|
| ResNet-50 | ~25M | ~1.2M (ImageNet) | ~24M |
| GPT-3 | ~175B | ~300B tokens* | very large |
| LLaMA-3 70B | ~70B | >1T tokens | very large |

*token count ≠ independent constraints directly*

On this manifold $\mathcal{M}^*$, **every point is a good solution** — low loss, gradient $\approx 0$. Gradient descent reaching any point on it "stops" in a good sense, rather than having to find one isolated point among countless local minima.

### Null space — the freedom to move

Why does the manifold appear? Consider the linearization of the network around $\theta_0$:

$$
f_\theta(x) \approx f_{\theta_0}(x) + J(\theta - \theta_0)
$$

where $J \in \mathbb{R}^{n \times |\theta|}$ is the **Jacobian** — $n$ rows (data points), $|\theta|$ columns (parameters). The data-fitting condition becomes the linear system $J(\theta - \theta_0) = y - f_{\theta_0}$. When $|\theta| \gg n$, $J$ has full row rank $= n$, and the general solution is:

$$
\theta = \theta_{\text{particular}} + \theta_{\text{null}}, \qquad \theta_{\text{null}} \in \ker(J)
$$

**The null space $\ker(J)$ is the "freedom to move"**: moving within the null space (of dimension $|\theta| - n$) doesn't change the network's output → doesn't change the loss. This is the geometric reason the solution set is a connected region, not isolated points.

### Neural Tangent Kernel (Jacot et al., 2018)

In the infinite-width limit, the Jacobian $J$ is **stable** throughout training (doesn't change with $\theta$). Then $K = JJ^T \in \mathbb{R}^{n \times n}$ is a fixed kernel, and training dynamics become:

$$
\frac{d\mathcal{L}}{dt} = -K \cdot \mathcal{L} \quad \Rightarrow \quad \text{linear, exponential convergence}
$$

This system is **linear and convex** — overparameterization turns the nonlinear problem into one with an analytic solution and guaranteed convergence to the global minimum. This is the limiting case that proves why overparameterization guarantees convergence.

<Note type="warning">
NTK is exact only at infinite width. Real finite-width networks **leave the NTK regime** — $J$ changes during training (feature learning happens). NTK is a useful approximation, not a full description of real networks.
</Note>

### Double Descent — breaking the bias-variance tradeoff

Classical wisdom says: increasing model complexity → overfitting → test error rises (the U-curve). **Belkin et al. (2019)** showed this is only partly true:

- **Underparameterized** ($|\theta| < n$): the classical U-curve holds — the model picks one of few solutions, easy to pick wrong
- **Interpolation threshold** ($|\theta| = n$): just enough freedom to fit even the noise → solution extremely sensitive to data → **test error spikes**
- **Overparameterized** ($|\theta| \gg n$): countless solutions on the manifold → GD picks the "simplest" one → **test error falls again** and keeps dropping as parameters are added

The test-error curve has **two descents** (double descent) — with the spike sitting exactly at $|\theta| = n$. This is why enormous models (deep in the overparameterized regime) generalize well rather than overfit as classical theory predicts. **Nakkiran et al. (2019)** confirmed the phenomenon occurs both with model size and with training epochs.

<DoubleDescentViz lang="en" />

### Implicit bias — which solution does GD pick?

When there are countless solutions on $\mathcal{M}^*$, which does gradient descent choose? **Soudry et al. (2018)** and **Gunasekar et al. (2017)** proved: on overparameterized linear models, GD converges to the **minimum-norm** solution:

$$
\theta^* = \arg\min \|\theta\| \quad \text{s.t.} \quad f_\theta(x_i) = y_i \;\; \forall i
$$

This is equivalent to **L2 regularization as $\lambda \to 0$** — yet another implicit form of regularization, added by no one. The minimum-norm solution tends to be smooth and to fit less noise → generalizes well. This is the final piece explaining double descent: in the overparameterized regime, GD's implicit bias automatically prefers the simpler solution.

<Note type="result">
Three forms of **implicit** regularization appear throughout this article — none added explicitly to the loss: (1) SGD noise penalizes sharp regions, (2) SGD's volume bias toward flat minima, (3) GD's minimum-norm preference in the overparameterized regime. Deep learning regularizes itself.
</Note>

## The full picture

Four phenomena — high dimensionality, the solution manifold, SGD noise, and overparameterization — resonate together to produce good behavior that convexity theory never predicted:

| Observation | Mechanism | Consequence |
|---|---|---|
| Most critical points are saddles, not local minima | High dimensionality + Wigner law (§saddle) | Bad local minima are extremely rare |
| Many local minima have equivalent low loss | Symmetry + overparameterization | No need to find the global minimum |
| Good minima are connected | Loss landscape is a connected manifold | Gradient descent doesn't get trapped in good regions |
| Flat minima generalize better | SGD noise + volume in parameter space | SGD + small batch naturally prefers generalization |
| Large models generalize rather than overfit | Overparameterization + implicit minimum-norm bias | Double descent — scale is a friend, not a foe |

## Why modern research doesn't check convexity

Today's deep learning papers almost never analyze the convexity of the loss. Not because it's unimportant — but because the question has changed:

| Reason | Explanation |
|---|---|
| We know it's non-convex | Obvious for any nonlinear network — no need to re-prove it |
| Tight theoretical guarantees aren't needed | Experiments prove it instead of theory — what works gets used |
| The question has shifted | No longer "does it converge?" but "where does it converge, and how does it generalize?" |
| Scale solves the problem | The larger the model, the "tamer" the landscape in practice (see NTK, double descent) |

## Theory running behind practice

This is what makes deep learning anomalous in the entire history of applied mathematics: **people used it before understanding why it works.** Normally theory comes first (proving sufficient conditions), then applications follow. Deep learning reversed that order.

Some theoretical results are trying to catch up:

- **Neural Tangent Kernel**: at infinite width, the network behaves like a kernel method → the problem becomes convex. But real finite-width networks leave this regime.
- **Loss landscape theory**: spin-glass, mode connectivity, simplexes — actively researched, but no complete description for real transformers yet.
- **Grokking** (Power et al., 2022): models suddenly generalize long after overfitting — still not fully understood why the landscape shifts so late.
- **Implicit bias for nonlinear networks**: the minimum-norm solution is proven for linear models, but for nonlinear networks it depends on architecture and isn't fully characterized.

## Summary

In introductory courses we check convexity because it is the **sufficient condition** for optimization to work — and was the only theoretical foundation we had at the time. Deep learning breaks that condition, but compensates with **overparameterization + SGD noise + high dimensionality** — which resonate to produce good behavior that convexity theory never predicted.

<Note type="paper">
Convexity still matters for understanding **why** gradient descent works in the simple case. But deep learning is an entirely different regime — current theory doesn't fully explain it, and runs 5–10 years behind empirical practice. That's why the defining question of deep learning isn't "does it work?" but "why does it work?"
</Note>

## References

| Paper | |
|---|---|
| Dauphin et al. (2014) | Identifying and Attacking the Saddle Point Problem |
| Choromanska et al. (2015) | The Loss Surfaces of Multilayer Networks |
| Keskar et al. (2017) | On Large-Batch Training for Deep Learning |
| Garipov et al. (2018) | Loss Surfaces, Mode Connectivity, and Fast Ensembling |
| Draxler et al. (2018) | Essentially No Barriers in Neural Network Energy Landscape |
| Benton et al. (2021) | Loss Surface Simplexes for Mode Connecting Volumes |
| Jacot et al. (2018) | Neural Tangent Kernel: Convergence and Generalization |
| Jin et al. (2017) | Escape Saddle Points Efficiently with Perturbed GD |
| Du et al. (2017) | Gradient Descent Can Take Exponential Time to Escape Saddle Points |
| Hochreiter & Schmidhuber (1997) | Flat Minima |
| Smith & Le (2018) | A Bayesian Perspective on Generalization and SGD |
| Zhu et al. (2019) | The Anisotropic Noise in SGD |
| Blanc et al. (2020) | Implicit Regularization for Deep Neural Networks |
| Belkin et al. (2019) | Reconciling Modern ML Practice and Bias-Variance Tradeoff |
| Nakkiran et al. (2019) | Deep Double Descent |
| Soudry et al. (2018) | The Implicit Bias of Gradient Descent on Separable Data |
| Gunasekar et al. (2017) | Implicit Regularization in Matrix Factorization |
| Power et al. (2022) | Grokking: Generalization Beyond Overfitting |
